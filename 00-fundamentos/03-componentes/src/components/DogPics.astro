---
interface Props {
  urls: string[];
}

const { urls } = Astro.props;
---

<div>
  {
    urls.map((dogImageUrl: string) => (
      <img
        src={dogImageUrl}
        alt="Random Dog"
        style="max-width:400px;height:auto;"
      />
    ))
  }
</div>
